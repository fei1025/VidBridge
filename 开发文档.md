# Codex 开发任务：局域网视频播放器

你需要开发一款以 Android 为第一目标平台的局域网视频播放器。

该软件主要用于播放以下来源的视频：

* Windows 共享文件夹
* macOS 共享文件夹
* NAS 共享目录
* Android 手机本地视频

当前阶段只开发移动端，不考虑 Windows、macOS 和 Web 桌面客户端。

---

# 一、项目目标

使用 Flutter 开发一个可运行的 Android 应用，实现以下完整链路：

```text
添加 SMB 服务器
→ 输入服务器地址和账号
→ 连接共享目录
→ 浏览文件夹
→ 选择视频
→ 使用 VLC 播放
→ 支持进度拖动
→ 保存播放记录
```

第一阶段重点是“真正可以播放局域网共享视频”，不要优先开发海报墙、影视刮削等非核心功能。

---

# 二、技术栈

优先采用以下技术方案：

```text
Flutter
Dart
flutter_vlc_player
Riverpod
go_router
Drift / SQLite
flutter_secure_storage
```

SMB 文件访问要求支持：

```text
SMB 2
SMB 3
用户名密码认证
匿名访问
目录浏览
文件信息读取
```

需要调研并选择一个适合 Flutter Android 的 SMB2/SMB3 实现。

SMB 模块必须通过接口抽象，不能让页面直接依赖具体 SMB 插件。

定义类似以下接口：

```dart
abstract interface class RemoteFileSystem {
  Future<void> connect();

  Future<List<RemoteFileItem>> listDirectory(String path);

  Future<RemoteFileInfo> getFileInfo(String path);

  Future<bool> exists(String path);

  Future<void> disconnect();
}
```

实现：

```dart
class SmbRemoteFileSystem implements RemoteFileSystem {
  // SMB2 / SMB3 implementation
}
```

后续即使更换 SMB 库，也不能影响页面层和播放器层。

---

# 三、项目结构

使用 Feature First 结构：

```text
lib/
├── app/
│   ├── app.dart
│   ├── router.dart
│   └── theme.dart
│
├── core/
│   ├── database/
│   ├── storage/
│   ├── errors/
│   ├── logging/
│   └── utils/
│
├── features/
│   ├── home/
│   ├── servers/
│   ├── file_browser/
│   ├── player/
│   ├── history/
│   ├── favorites/
│   └── settings/
│
├── remote_file_system/
│   ├── remote_file_system.dart
│   ├── models/
│   └── smb/
│
└── shared/
    ├── widgets/
    └── models/
```

保持以下分层：

```text
presentation
domain
data
```

页面不能直接访问 SQLite、SMB 插件或者 flutter_secure_storage。

---

# 四、第一阶段开发内容

## 1. 创建基础项目

完成：

* Flutter Android 项目初始化
* Material 3
* 深色和浅色主题
* Riverpod 状态管理
* go_router 路由
* Drift 本地数据库
* 全局异常处理
* 日志工具
* Android 网络权限
* Android 局域网访问相关配置

应用名称暂定：

```text
LanPlayer
```

包名使用占位配置，确保后续可以统一修改。

---

## 2. 服务器管理

实现“网络位置”页面。

服务器数据结构：

```dart
class ServerConfig {
  final String id;
  final String name;
  final String host;
  final int port;
  final String protocol;
  final String? shareName;
  final String? username;
  final String credentialId;
  final String? initialPath;
  final DateTime createdAt;
  final DateTime updatedAt;
}
```

第一版协议只开放：

```text
SMB
```

添加服务器表单字段：

```text
服务器名称
IP 地址或主机名
端口，默认 445
共享名称
用户名
密码
初始目录
是否匿名访问
```

提供以下功能：

* 添加服务器
* 编辑服务器
* 删除服务器
* 测试连接
* 显示连接结果
* 保存最近连接时间
* 保存最近访问目录

账号密码不能明文保存在 SQLite。

密码使用：

```text
flutter_secure_storage
```

SQLite 只保存 `credentialId`。

日志中禁止输出：

* 用户密码
* 带密码的 SMB 地址
* 完整认证参数

---

## 3. SMB 文件浏览

连接服务器后进入文件浏览页面。

需要支持：

* 浏览共享目录
* 返回上级目录
* 面包屑导航
* 下拉刷新
* 连接失败重试
* 目录加载状态
* 空目录提示
* 文件名搜索
* 按名称排序
* 按修改时间排序
* 按文件大小排序
* 文件夹优先显示
* 保存最后访问路径

文件结构：

```dart
class RemoteFileItem {
  final String name;
  final String fullPath;
  final bool isDirectory;
  final int? size;
  final DateTime? modifiedAt;
  final String? extension;
}
```

第一版只在列表中显示常见视频文件：

```text
mp4
mkv
avi
mov
wmv
flv
webm
m4v
ts
m2ts
mpeg
mpg
3gp
```

字幕文件可以显示特殊图标：

```text
srt
ass
ssa
vtt
sub
```

目录浏览必须支持：

* 中文文件名
* 空格
* 特殊符号
* 较深的目录层级
* 大量文件
* 文件名相同但路径不同的情况

不要一次性对所有视频读取媒体时长，这会导致目录打开速度过慢。

---

## 4. VLC 播放器

使用 `flutter_vlc_player` 作为播放器。

第一版优先验证 VLC 能否直接打开：

```text
smb://服务器/共享目录/视频文件
```

播放时需要正确处理：

* 用户名
* 密码
* SMB 共享名称
* 中文路径
* URL 编码
* VLC 网络缓存参数
* 硬件解码

不要在播放器页面或者日志中展示包含明文密码的地址。

播放器页面功能：

* 播放
* 暂停
* 进度拖动
* 当前时间
* 视频总时长
* 快进 10 秒
* 快退 10 秒
* 横屏全屏
* 锁定控制界面
* 倍速播放
* 音轨切换
* 字幕轨道切换
* 画面比例切换
* 播放错误提示
* 加载缓冲提示
* 重新连接
* 退出播放

倍速支持：

```text
0.5
0.75
1.0
1.25
1.5
2.0
```

画面比例支持：

```text
自动
原始比例
16:9
4:3
填充
适应屏幕
```

Android 返回键规则：

```text
控制栏显示时
→ 先隐藏控制栏

横屏全屏时
→ 先退出全屏

普通播放页面
→ 返回文件列表
```

---

## 5. 播放地址策略

首先实现 VLC 直接播放 SMB 地址。

如果 `flutter_vlc_player` 无法稳定处理 SMB 登录、Seek 或认证，则保留播放器接口，增加本地 HTTP Range 代理作为备用方案。

播放器入口必须抽象：

```dart
abstract interface class PlaybackSourceResolver {
  Future<PlaybackSource> resolve(RemoteFileItem file);
}
```

播放源：

```dart
class PlaybackSource {
  final String uri;
  final Map<String, String> headers;
  final PlaybackSourceType type;
}
```

类型：

```dart
enum PlaybackSourceType {
  localFile,
  smbDirect,
  localHttpProxy,
}
```

第一阶段先完成：

```text
localFile
smbDirect
```

不要一开始实现复杂的完整视频下载后再播放。

禁止以下逻辑：

```text
先把几 GB 视频完整下载到手机
→ 下载完成后播放
```

---

## 6. 播放记录

使用 Drift 保存播放记录。

数据结构：

```dart
class PlaybackHistory {
  final String id;
  final String serverId;
  final String shareName;
  final String remotePath;
  final String fileName;
  final int? fileSize;
  final DateTime? fileModifiedAt;
  final Duration position;
  final Duration duration;
  final DateTime lastPlayedAt;
  final bool completed;
}
```

文件唯一标识优先使用：

```text
serverId
+ shareName
+ fullPath
+ fileSize
+ modifiedAt
```

播放过程中每隔一段时间保存进度，同时在以下时机立即保存：

* 暂停
* 退出播放器
* 应用进入后台
* 视频播放完成
* 播放发生错误

播放完成判断：

```text
播放进度超过总时长的 90%
```

重新播放时：

```text
进度小于 30 秒
→ 从头播放

进度超过 30 秒且未完成
→ 提示继续播放或从头播放

已经完成
→ 默认从头播放
```

---

## 7. 首页

首页先实现简单列表，不做复杂海报墙。

页面内容：

```text
继续播放
最近播放
收藏
网络位置
本地视频
```

“继续播放”只显示未完成的视频。

最近播放按 `lastPlayedAt` 倒序排列。

点击历史记录时：

1. 检查服务器配置是否存在。
2. 检查文件是否仍然存在。
3. 重新建立 SMB 连接。
4. 从保存进度继续播放。
5. 文件不存在时提示用户，并允许删除历史记录。

---

## 8. 收藏功能

允许用户收藏：

* 视频文件
* 文件夹

收藏数据包含：

```text
服务器 ID
共享名称
完整路径
文件名称
类型
创建时间
```

点击收藏后直接打开文件或进入对应文件夹。

---

## 9. 设置页面

第一版设置项：

```text
默认硬件解码模式
默认播放倍速
默认画面比例
网络缓存时长
是否记忆播放进度
播放完成百分比
是否自动横屏
是否保持屏幕常亮
是否显示隐藏文件
```

网络缓存提供几个预设值：

```text
低延迟
均衡
稳定
自定义
```

不要让页面直接保存 VLC 参数字符串。

将设置转换为 VLC 参数的逻辑放在播放器数据层。

---

# 五、UI 要求

整体风格参考现代视频播放器和文件管理器，不要使用默认 Flutter 示例页面风格。

要求：

* Material 3
* 深色模式优先
* 页面信息密度适中
* 手机单手操作友好
* 播放器按钮点击区域足够大
* 加载、错误、空状态完整
* 文件名过长时省略
* 文件路径可查看完整内容
* 横屏播放器隐藏系统多余区域

网络位置卡片显示：

```text
服务器名称
IP 地址
共享名称
最近连接时间
连接状态
```

文件列表显示：

```text
文件图标
文件名
文件大小
修改时间
```

不要在第一版实现复杂动画。

---

# 六、错误处理

定义统一错误类型：

```dart
sealed class AppFailure {}

class AuthenticationFailure extends AppFailure {}

class ConnectionFailure extends AppFailure {}

class TimeoutFailure extends AppFailure {}

class FileNotFoundFailure extends AppFailure {}

class PermissionDeniedFailure extends AppFailure {}

class UnsupportedProtocolFailure extends AppFailure {}

class PlaybackFailure extends AppFailure {}
```

用户提示需要可理解，不能直接显示底层异常堆栈。

示例：

```text
无法连接服务器，请检查 IP 地址和端口。

用户名或密码错误。

共享目录不存在或无权访问。

视频文件已经被移动或删除。

网络连接已中断，正在尝试重新连接。

当前视频格式或编码不受设备支持。
```

开发日志可以保留异常堆栈，但不能包含密码。

---

# 七、测试要求

至少添加以下单元测试：

* SMB 地址拼接
* 特殊字符路径编码
* 视频扩展名识别
* 播放记录唯一标识生成
* 播放完成判断
* 密码不进入数据库
* 文件大小格式化
* 播放时间格式化
* 最近播放排序
* 设置到 VLC 参数的转换

至少添加以下 Widget 测试：

* 添加服务器表单校验
* 服务器列表空状态
* 文件列表加载状态
* 文件列表错误状态
* 播放历史列表
* 继续播放提示框

---

# 八、MVP 验收标准

第一阶段完成后，必须满足以下验收条件：

```text
1. 可以安装并运行在 Android 真机。
2. 可以添加一个 SMB2/SMB3 服务器。
3. 可以验证用户名和密码。
4. 可以浏览 Windows 或 NAS 的共享目录。
5. 可以识别目录中的 MKV 和 MP4 视频。
6. 点击视频后可以使用 VLC 开始播放。
7. 可以拖动到视频中间继续播放。
8. 可以暂停和恢复。
9. 可以切换内嵌字幕和音轨。
10. 可以保存播放位置。
11. 退出后再次进入可以继续播放。
12. 密码没有明文写入 SQLite 或日志。
13. SMB 连接失败时应用不会崩溃。
14. 视频播放失败时可以返回文件列表。
```

使用以下类型文件测试：

```text
普通 MP4
5GB 以上 MKV
H.264 视频
H.265 视频
多音轨 MKV
内嵌字幕 MKV
中文文件名视频
较深目录中的视频
```

---

# 九、Codex 执行规则

按照阶段逐步开发，不要一次生成大量无法验证的代码。

执行顺序：

```text
阶段 1：项目骨架和数据库
阶段 2：服务器管理
阶段 3：SMB 连接和目录浏览
阶段 4：VLC 播放验证
阶段 5：播放控制界面
阶段 6：播放历史和收藏
阶段 7：设置和错误处理
阶段 8：测试和文档
```

每完成一个阶段都必须：

1. 运行 `flutter analyze`。
2. 修复所有错误。
3. 运行已有测试。
4. 汇报本阶段修改的文件。
5. 汇报仍然存在的风险。
6. 给出下一阶段准备执行的内容。

禁止：

* 用纯 Mock 页面假装功能完成。
* 为了通过编译删除核心功能。
* 将账号密码写死。
* 将大视频完整下载后再播放。
* 页面直接操作 SMB 插件。
* 页面直接操作 SQLite。
* 捕获异常后完全忽略。
* 创建多个作用相同的 Service。
* 在没有验证的情况下声称 SMB 播放成功。
* 为尚未使用的功能创建大量空类。

遇到插件 API 不确定时：

1. 检查当前项目实际安装版本。
2. 阅读插件源码和示例。
3. 以实际 API 为准。
4. 不要凭空编造方法名称。
5. 无法实现时保留接口并明确说明阻塞原因。

---

# 十、第一阶段最终交付内容

Codex 最终需要交付：

```text
可运行的 Flutter Android 项目
README.md
项目架构说明
SMB 配置说明
Windows 共享文件夹配置说明
NAS 连接测试说明
Android 打包说明
已实现功能列表
未实现功能列表
已知问题列表
测试结果
```

README 中需要说明：

* 如何启动项目
* 如何连接 Windows 共享目录
* 如何连接 NAS
* 如何打开 SMB2/SMB3
* 如何填写共享名称
* 如何测试大文件播放
* 如何排查连接失败
* 如何修改包名和应用名称

---

# 十一、后续开发计划

下面内容不属于第一阶段 MVP，第一阶段完成并稳定后再开发。

## 第二阶段：播放器体验增强

增加：

```text
双击快进和快退
左右滑动调整进度
左侧滑动调整亮度
右侧滑动调整音量
画中画
播放锁定
耳机按键控制
后台恢复
自动播放下一集
文件夹连续播放
播放列表
睡眠定时
```

## 第三阶段：网络能力增强

增加：

```text
SMB 服务器自动发现
局域网设备扫描
DLNA / UPnP
WebDAV
NFS
SFTP
HTTP 文件服务器
断线自动重连
NAS 唤醒
连接测速
```

## 第四阶段：本地 HTTP Range 代理

当 VLC 直接播放 SMB 存在以下问题时再实现：

```text
部分 NAS 无法认证
进度拖动失败
插件无法传递 SMB 凭据
长时间播放容易断开
无法控制预读和缓存
```

代理架构：

```text
VLC
→ http://127.0.0.1:端口/video/token
→ 本地 Range Server
→ SMB 分段读取
→ NAS
```

需要支持：

```text
HTTP 206
Content-Range
Accept-Ranges
分段读取
Seek
预读
缓存
取消旧请求
断线重连
临时访问 Token
```

## 第五阶段：影视媒体库

增加：

```text
文件夹海报墙
影片信息
电视剧季和集识别
本地 NFO
本地海报读取
影视信息刮削
演员信息
视频缩略图
观看状态
剧集自动连播
```

## 第六阶段：iOS

在 Android 版本稳定后适配 iOS：

```text
iOS 局域网权限
SMB 原生依赖打包
VLC iOS 播放
安全存储
横屏
画中画
后台恢复
App Store 权限说明
```

业务层、数据库层和页面层应尽量复用，平台差异放到独立适配层中。

---

# 十二、现在开始执行

现在先不要实现所有后续计划。

从以下任务开始：

```text
1. 检查当前项目，如果项目不存在则创建 Flutter 项目。
2. 初始化 Riverpod、go_router、Drift 和安全存储。
3. 建立 Feature First 目录结构。
4. 创建服务器配置模型和数据库表。
5. 完成网络位置列表页面。
6. 完成添加和编辑 SMB 服务器页面。
7. 完成表单校验。
8. 完成密码安全存储。
9. 运行 flutter analyze 和测试。
10. 汇报已完成内容和下一阶段工作。
```

完成以上内容后停止，不要直接跳到播放器和影视墙开发。
